#pragma once

// Define a classe Fatorial para calcular o fatorial de um número inteiro.
class Fatorial
{
	public:
		// Lê um numero inteiro, calcula e exibe o fatorial desse número.
		int fatorial();

		// Calcula o fatorial de um numero inteiro.
		int fatorial(int numero);
};

